// Retired in the consolidation pass. The order-lifecycle view duplicated the
// existing Deal Desk / Payment Gate / Production Center / Dispatch pages, so its
// data was folded into those pages (one capability per home). Kept as an empty
// module to avoid a dangling import; safe to delete.
export {};

